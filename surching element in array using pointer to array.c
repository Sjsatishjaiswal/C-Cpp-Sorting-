//wap to find to searching element of array
int main ()
{
    int a[100],n,i,count,w;
    int *p;
    printf("Enter element \n ");
    scanf("&d",&n);
    for (i=0;i<n;i++)
    {
        print ("Enter value :n");
        scanf("%d",&a[i]);

    }
    printf("enter value: ");
    scanf("%d",&w);
    p=&a[0];
    for(i=0;i<n;i++)
        printf("Enter value :n");
    scanf("%d",*p+i);
    {
        if (count==0);
        printf("Not found");
    }
}
